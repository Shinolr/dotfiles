disabled_rules:
  - force_try
  - force_cast
  - function_body_length
#   - line_length
#   - multiple_closures_with_trailing_closure

identifier_name:
  allowed_symbols: "_"
  excluded:
    - id
# type_name:
#   excluded:
#     - S

# parent_config: .swiftlint.yml